<!DOCTYPE html>
<html lang="en">form
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/postinfo2" method="post" name="postinfo">

        아이디<input 
        type="text"
        name="id" 
        placeholder="아이디"
        pattern="^([a-z0-9]){6,}$" 
        title="소문자,숫자, 6자리 이상"
        required
        />

        비밀번호<input 
        type="password"
        name="pw" 
        placeholder="비밀번호"
        pattern="^([a-z0-9!@#$%^&*]){8,12}$"
        title="소문자,숫자,8자리이상 12자리 이하"
        required
        />
        이름<input type="text" name="name" required>
        <fieldset >
            <legend>
                성별
            </legend>
            <input type="radio" name="gender" value="man" required>남자
            <input type="radio" name="gender" value="man" required>여자

        </fieldset>

        <fieldset>
            <legend>
                생년월일
            </legend>
            <select name="birthYear">
                <% for (var i=1900; i<=2022; i++) {%>
                <option value="<%=i%>"><%=i%></option>
                
                <%} %>
            </select>
            년
            <select name="birthMonth">
                <% for (var i=1; i<=12; i++) {%>
                <option value="<%=i%>"><%=i%></option>
                <%} %>
            </select>
            월
            <select name="birthDay">
                <% for (var i=1; i<=31; i++) {%>
                <option value="<%=i%>"><%=i%></option>
                <%} %>
            </select>
            일


        </fieldset>

        <fieldset>
            <legend>
                관심사
            </legend>
            <input type="checkbox" name="interest" value="travel">여행
            <input type="checkbox" name="interest" value="fession">패션
            <input type="checkbox" name="interest" value="food">음식

        </fieldset>

        <fieldset>
            <legend>
                선택
            </legend>
            <input type="color" id="color" name="color" value="#CCFFFF">
            <input id="number" type="number" value="" >

        </fieldset>


        <button type="submit">회원가입</button>
    </form>

</body>
</html>